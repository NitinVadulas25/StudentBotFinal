package com.github.mittyrobotics.drivetrain;

import com.github.mittyrobotics.util.math.Angle;

import static java.lang.Math.PI;

public class unnec {
//    boolean clock = (values[i] - getStandardizedModuleAngle(i) < PI && values[i] - getStandardizedModuleAngle(i) > 0) || values[i] - getStandardizedModuleAngle(i) < -PI;
//    boolean flip = Angle.getRealAngleDistance(getStandardizedModuleAngle(i), values[i], clock) > PI / 2;
//
//    flipped[i] = flip;
//
//    values[i] = anglemotors[i].getSelectedSensorPosition() + (clock ? 1 : -1) * Angle.getRealAngleDistance(getStandardizedModuleAngle(i), values[i], clock) + (flip ? (clock ? -1 : 1) * PI : 0);

}
